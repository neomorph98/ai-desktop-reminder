# 复制本文件为 .env 并按需填写

# === 选择 LLM 供应商 ===
# 可选：deepseek | glm | minimax | qwen | moonshot | custom
LLM_PROVIDER=deepseek

# === 各家 API key（用哪个填哪个即可，留空的不影响）===
DEEPSEEK_API_KEY=
GLM_API_KEY=
MINIMAX_API_KEY=
DASHSCOPE_API_KEY=     # 通义千问 Qwen
MOONSHOT_API_KEY=      # Kimi

# === 模型选型建议（重要）===
# 本工具的活儿 = 把一句话抽取成结构化 JSON，属于「轻量抽取 / 分类」。
# 👉 建议用【快、便宜的小模型】（各供应商默认值已替你选好，一般无需改）：
#      minimax  -> abab6.5s-chat            deepseek -> deepseek-chat
#      glm      -> glm-4-flash（有免费额度）   qwen     -> qwen-plus
#      moonshot -> moonshot-v1-8k
# 🚫 不建议用【推理模型】（如 MiniMax-M 系列、DeepSeek-R 系列等）：
#      它们会先输出一大段 <think> 思维链，对这种简单抽取属"杀鸡用牛刀"——
#      更慢、更贵、解析也更不稳。推理模型适合数学/代码/多步推理等难题，不适合本场景。

# === 可选覆盖（自建 / 中转 / 换模型时用，优先级最高）===
# LLM_API_KEY=         # 直接指定 key，会覆盖上面按供应商取 key
# LLM_MODEL=           # 覆盖默认模型，如 glm-4-plus / qwen-max（仍建议选小模型，别选推理模型）
# LLM_BASE_URL=        # 覆盖 base_url（任意 OpenAI 兼容端点）
# 注：MiniMax 的 base_url / model 可能因账号而异，按其控制台文档调整。

# === 全局快捷键 ===
HOTKEY=ctrl+alt+s

# === 桌面壁纸提醒（Lively，可选）===
# WALLPAPER_PORT=8765   # 本地壁纸数据服务端口（在 Lively 里添加 http://127.0.0.1:此端口/）
# SHOW_OVERLAY=1        # 是否显示右上角小窗；用 Lively 壁纸时可设 0 隐藏
